import React from "react";


function ProfileCard({title,j}){
        return (
            <div>
                <div>Title is {title} and handle is {j}</div>
            </div>
        );
}

export default ProfileCard